export type Post = {
  id: string;
  slug: string;
  title: string;
  excerpt: string;
  publishedAt: string;
};

export type ContactFormData = {
  name: string;
  email: string;
  subject: string;
  message: string;
};
